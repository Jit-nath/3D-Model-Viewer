import React from 'react';

const Modifiers: React.FC = () => {
    return (
        <div>
            <h2>Modifiers</h2>
            <p>This is a mock component for the Modifiers sidebar.</p>
        </div>
    );
};

export default Modifiers;